# ignore the build folder we use for CMake
build

# ignore the mac ds store file
**/.DS_Store

# ignore the app folder where the binaries are stored
app

# ignore the auto-generated sublime-workspace file
c-snake-raylib.sublime-workspace